ARCHIVOS A REEMPLAZAR - Apartado de Envio en Pedidos
======================================================

Copia cada archivo a la ruta EXACTA que se indica dentro de tu
proyecto en C:\Users\salva\Desktop\SINERGIA\fruteria-os\fruteria-os\

Cuando Windows pregunte si reemplazar, dile que SI a todos.

BACKEND (7 archivos):
----------------------
1. server\src\models\producto.model.js
   -> server\src\models\producto.model.js

2. server\src\services\producto.service.js
   -> server\src\services\producto.service.js

3. server\src\services\pedido.service.js
   -> server\src\services\pedido.service.js

4. server\src\controllers\producto.controller.js
   -> server\src\controllers\producto.controller.js

5. server\src\routes\producto.routes.js
   -> server\src\routes\producto.routes.js

6. server\src\index.js
   -> server\src\index.js

7. server\db\schema.sql
   -> server\db\schema.sql

FRONTEND (2 archivos):
------------------------
8. client\src\pages\Pedidos.jsx
   -> client\src\pages\Pedidos.jsx

9. client\package.json
   -> client\package.json
   (trae vite corregido a la version 7, necesario para que compile)

DESPUES DE COPIAR TODO
========================

1. Verifica que quedo bien:
   cd C:\Users\salva\Desktop\SINERGIA\fruteria-os\fruteria-os
   findstr "Cobrar envío" client\src\pages\Pedidos.jsx

   Debe mostrar una linea con ese texto. Si no muestra nada, algo
   no se copio bien.

2. Sube los cambios:
   git add .
   git status
   (revisa que aparezcan los 9 archivos como modificados)
   git commit -m "Feature: apartado de envio en pedidos"
   git push

3. Corre la migracion SQL en Railway (consola del backend, sistema-kioskos):
   node -e "require('dotenv').config(); const mysql = require('mysql2/promise'); (async () => { const conn = await mysql.createConnection(process.env.MYSQL_URL); await conn.execute('ALTER TABLE productos ADD COLUMN es_envio BOOLEAN NOT NULL DEFAULT FALSE'); console.log('Columna es_envio agregada correctamente.'); await conn.end(); })().catch(e => console.error('ERROR:', e.message));"

   Si dice "Duplicate column name", ya estaba aplicada, no hay problema.

4. Espera a que Railway (backend) y Netlify (frontend) terminen de
   redesplegar (1-3 minutos cada uno), y recarga el sistema con
   Ctrl + Shift + R.
