# Puerto del servidor
PORT=3001

# Conexion a MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=fruteria_os

# Secreto para firmar los tokens JWT (cambiar en produccion)
JWT_SECRET=fruteria_secret_cambia_esto

# Origen permitido para CORS (el frontend en produccion)
CLIENT_ORIGIN=http://localhost:5173
